[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."news"]
  timeout = 30

[functions."news-scheduler"]
  timeout = 30
  schedule = "0 */2 * * *"

[functions]
  node_bundler = "esbuild"
